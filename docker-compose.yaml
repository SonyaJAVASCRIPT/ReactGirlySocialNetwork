services:
  backend:
    build: ./backend
    ports:
      - "6969:6969"
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: rgsn
      POSTGRES_PASSWORD: rgsn
      POSTGRES_DB: rgsn-db
    volumes:
      - girlynetwork:/var/lib/postgresql
volumes:
  girlynetwork:
